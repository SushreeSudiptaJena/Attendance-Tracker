# Copy this file to "keystore.properties" (which is gitignored) and fill in your
# own values to build a signed release. NEVER commit the real keystore.properties
# or the .jks file.
#
# storeFile can be an absolute path or relative to the project root.
storeFile=my-upload-key.jks
storePassword=CHANGE_ME
keyAlias=upload
keyPassword=CHANGE_ME
